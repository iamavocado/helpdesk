# DOZZIER HelpDesk Mobile â€” plantilla de variables de entorno.
# Copia este archivo a .env.development / .env.staging / .env.production
# y completa los valores. NO se versionan los .env.* reales (ver .gitignore).

# Ambiente lÃ³gico de la app (development | staging | production)
APP_ENV=development

# URL base de la API REST del cliente (vacÃ­o => se usa el mock local)
API_BASE_URL=

# Usar el mock server (MSW) en lugar de la API real (true | false)
USE_MOCK_API=true

# Activar certificate pinning (true en staging/production)
ENABLE_CERT_PINNING=false

# Minutos de inactividad antes del logout automÃ¡tico
INACTIVITY_TIMEOUT_MINUTES=30

# Dominio para el login de la API (campo `dominio` de /api/Auth/login)
DOMINIO=

# Solo para validaciÃ³n local del desarrollador (NO usado por la app).
# Credenciales de prueba para probar la API real desde scripts. BÃ³rralas luego.
DEV_USERNAME=
DEV_PASSWORD=
